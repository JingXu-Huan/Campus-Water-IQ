spring:
  application:
    name: iot-device
dubbo:
  application:
    name: iot-device
  registry:
    address: nacos://101.42.157.163:8848
    username: nacos
    password: nacos
    register-mode: instance
  protocol:
    name: tri
    port: 50052

# Actuator 配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
  prometheus:
    metrics:
      export:
        enabled: true